// Account and pipeline actions that need a word of reassurance or consequence.
export const ACCOUNT_TOOLTIPS = {
  passwordStorage:
    'Your password is encrypted using industry-standard hashing algorithms and is never stored in plain text.',
  markGhosted: 'Mark as ghosted, so it leaves the pipeline and this list.',
} as const;
